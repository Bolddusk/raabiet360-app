export enum KEYS {
  THEME = 'theme',
  LANGUAGE = 'language',
  AUTH = 'authData',
  USER = 'userInfo',
}

export enum THEME {
  LIGHT = 'light',
  DARK = 'dark',
}

export enum WorkerStatus {
  PENDING = 'Pending',
  COMPLETED = 'Completed',
  IN_PROGRESS = 'In-Progress',
}

export enum TOP_TABS_REQUESTS {
  RECENT = 'recent',
  CREATE = 'create',
}

export enum USER_ROLE {
  DRIVER = 'driver',
  WORKER = 'worker',
}

export enum TOP_TABS_CHECKIN {
  RECENT = 'recent',
  ONGOING = 'ongoing',
  COMPLETED = 'completed',
}

export enum HUMAN_RESOURCE_ROLE {
  DutyStaff = 'Duty Staff',
  Contractor = 'Contractor',
  SiteWorker = 'Site Worker',
}